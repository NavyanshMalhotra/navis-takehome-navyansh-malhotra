# ==============================================================================
# Nevis Forward Deployed Engineer (FDE) Agentic Pipeline Configuration
# ==============================================================================

# Google Gemini API Key
# Required for live agentic reasoning (gemini-2.5-flash) and semantic embeddings (text-embedding-004).
# Supports Google AI Studio keys (AIza...) and Google Cloud Vertex AI Express keys (AQ...).
GEMINI_API_KEY=

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# File Paths (relative to repository root)
SOURCES_DIR=nevis-fde-hometask/sources
OUTPUTS_DIR=outputs

